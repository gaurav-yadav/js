{
  "name": "curry",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "babel src/ -d dist/ && node dist/index.js",
    "test": "babel src/ -d dist/ && node dist/test/test.js",
    "babel-version": "babel --version"
  },
  "devDependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1"
  },
  "dependencies": {
    "fs": "0.0.1-security"
  }
}
